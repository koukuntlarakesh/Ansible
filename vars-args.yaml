- name: variables passing through args
  hosts: localhost
  tasks:
  - name: accessing from args
    ansible.builtin.debug:
      msg: "Hello your are learning {{Course}} Trainer {{Trainer}}  and duration {{Duration}}"